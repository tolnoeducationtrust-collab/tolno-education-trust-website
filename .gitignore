# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editor / OS junk
Thumbs.db
*.swp
*~

# Environment / secrets (never commit)
.env
.env.*
!.env.example

# Node (in case tooling is added later)
node_modules/
